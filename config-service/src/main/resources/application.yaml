server:
  port: 8081
spring:
  cloud:     config:
      server:
        git:
          default-label: confing-server
          uri: https://github.com/Anghelo79/microservicios-tutorial
          search-paths: config-data
  application:
    name: config-service